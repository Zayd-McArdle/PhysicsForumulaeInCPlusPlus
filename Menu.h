#pragma once
#include <iostream>
/// @brief Prints a list of different formulas
/// @param printList 
int printSectionMenu(void (*printList)());
/// @brief Asks the user what element of the formula that they want to calculate.
/// @param printList 
int printFormulaMenu(void (*printList)());